#include <fstream>
#include <string>
#include<conio.h>
using namespace std;

int main()

{
  string name; 
  cout << "What is your name? ";
  cin >> name;
  cout << "Hi " << name << "\n";
  
  
	int arr[130], size, insert, i, pos;
	cout<<"Please Enter Array Size : ";
	cin>>size;
	cout<<"Enter Array elements : ";
	for(i=0; i<size; i++)
	{
		cin>>arr[i];
  }
}